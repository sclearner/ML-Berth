using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestinationNodes : MonoBehaviour
{
    // Start is called before the first frame update
    public List<Node> nodes;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
